# Memoria V2

> 一款专注于**视频缓存管理与混流导出**的桌面端工具，支持从本地或移动设备读取 B 站等平台的视频缓存文件，解析元数据、批量混流导出为通用格式。
> **V2.0.0版本已发布**：支持Bilibili平台，暂不支持其他平台

---

## 目录

- [概述](#概述)
- [核心功能](#核心功能)
- [主要特性](#主要特性)
- [快速开始](#快速开始)
- [外部设备导入使用指南](#外部设备导入使用指南)
- [安卓设备开启开发者模式](#安卓设备开启开发者模式)
- [缓存管理](#缓存管理)
- [导出格式支持](#导出格式支持)
- [开发环境](#开发环境)
- [许可证](#许可证)

---

## 概述

Memoria 是一款面向视频缓存文件管理的桌面应用程序，旨在帮助用户：

- **读取视频缓存文件** — 从本地文件系统或移动设备读取 B 站等平台的离线缓存
- **可视化元数据** — 解析并以表格形式展示标题、UP主、BV号、分P、分辨率、弹幕数等信息
- **混流导出** — 将分离的音视频流（.m4s）合并为 MP4 / MKV / MOV / WEBM 等通用格式
- **批量处理** — 支持多文件同时导入与批量混流导出

---

## 核心功能

### 数据导入

| 导入方式 | 说明 |
|---------|------|
| 本地文件导入 | 从本地文件系统选择缓存文件夹，自动解析 entry.json / index.json |
| 外部设备导入（USB） | 通过 USB 数据线连接安卓设备，使用 ADB 拉取缓存文件 |
| 外部设备导入（WiFi） | 通过无线局域网连接安卓设备，无线调试 + ADB 拉取 |
| 独立音视频导入 | 手动选择独立的视频文件和音频文件进行混流 |

### 数据展示

- **18 列元数据表格** — 8 个强制列（序号、状态、标题、分P号、分P标题、视频路径、音频路径、进度）+ 10 个可选列（离线ID、BV号、视频类型、UP主、UP主UID、创建时间、时长、文件大小、弹幕数、分辨率）
- **列头自定义** — 在设置 → 列头设置中自由勾选可选列的显隐
- **右键菜单** — 支持独立导入、编辑标题、删除行、清空行等快捷操作

### 混流导出

- **批量导出** — 选择多行一次性提交混流任务，顺序执行
- **实时进度** — 表格内显示每行的导出进度百分比
- **FFmpeg 自检** — 启动时自动检测 FFmpeg 环境（优先系统 PATH，回退到随包 FFmpeg）

---

## 主要特性

- **零配置要求** — 内置 FFmpeg 与 ADB 环境，下载即用
- **多连接方式** — USB 有线 + WiFi 无线，适配不同使用场景
- **智能缓存管理** — 拉取的缓存文件自动存储于 AppData，支持过期清理与手动清理
- **跨平台音视频处理** — 基于 FFmpeg，支持流复制（秒级导出）与转码两种模式
- **Windows 文件名安全** — 自动清洗非法字符，避免导出失败

---

## 快速开始

### 环境要求

- Windows 10/11（64 位）
- 安卓设备（USB/WiFi 导入需要 Android 11+ 无线调试支持）

### 下载与安装

1. 从 [Release 页面](../../releases) 下载最新版本的压缩包
2. 解压到任意目录
3. 运行 `Memoria_N_V2.exe`

> 软件已内置 FFmpeg（`FFmpeg_tools/bin/`）和 ADB（`ADB_tools/bin/`），无需额外配置环境。

### 基本使用流程

```
导入缓存数据 → 表格预览元数据 → 选择导出行 → 设置输出格式 → 点击"混流导出"
```

1. 点击主界面的 **"本地文件导入"** 或 **"外部设备导入"** 按钮
2. 选择要导入的缓存文件夹（本地选择目录 / 外部设备浏览设备文件）
3. 数据进入表格后，勾选或选中要导出的行
4. 点击 **"混流导出"** 按钮，在输出设置对话框中选择格式和输出目录
5. 等待混流完成，进度实时显示在表格中

---

## 外部设备导入使用指南

### 一、USB 有线连接

**适用场景**：设备在手边，有 USB 数据线

#### 步骤

1. **手机端**：开启开发者选项 → 开启 **USB 调试**（详见 [下方各品牌指南](#安卓设备开启开发者模式)）
2. 用 **USB 数据线** 连接手机和电脑
3. 手机弹出 **"允许 USB 调试吗？"** → 勾选"始终允许" → 点 **确定**
4. 打开 Memoria，点击 **"外部设备导入"**
5. 点击 **"🔄 刷新设备"** — 设备列表应显示已连接的手机
6. 点击设备名称 → 自动浏览 B 站缓存目录
7. 选中目标文件夹 → 点击 **"解析选中文件"** → 预览解析结果
8. 勾选要导入的项 → 点击 **"确认导入"** → 数据进入主表格

> **提示**：USB 连接无需配对/连接步骤，是最简单的测试方式。请确保使用支持数据传输的 USB 线（非纯充电线）。

### 二、WiFi 无线连接

**适用场景**：设备不在手边，或不想插线

#### 前置条件

- 手机和电脑在 **同一 WiFi 网络** 下
- 手机系统为 **Android 11 及以上**（支持无线调试功能）

#### 步骤

**第 1 步：配对（仅首次需要）**

1. 手机开发者选项 → 开启 **无线调试**
2. 点击 **"使用配对码配对设备"**
3. 手机屏幕显示：
   - **WiFi 配对码**：6 位数字（如 `348521`）
   - **配对地址**：如 `192.168.1.8:42171`
4. 在 Memoria 的外部设备导入窗口中，点击 **"🔗 配对"**
5. 第一个对话框输入 **配对地址**（`192.168.1.8:42171`）
6. 第二个对话框输入 **6 位配对码**
7. 状态栏显示 **"✓ 已配对，请连接"** → 配对成功

> **注意**：配对端口与连接端口**不同**！配对页面显示的端口仅用于配对。

**第 2 步：连接**

8. 退出配对码页面，回到 **无线调试主页面**
9. 页面底部显示 **连接地址**（如 `192.168.1.8:45123`）— 注意端口与配对不同
10. 在 Memoria 的连接输入框中输入此 **连接地址**
11. 点击 **"连接"** 按钮 → 状态栏显示 **"✓ 已连接"**

**第 3 步：使用**

12. 点击 **"🔄 刷新设备"** → 设备列表显示已连接的设备
13. 点击设备名称 → 自动浏览 B 站缓存目录
14. 后续操作与 USB 连接相同

#### 常见问题

| 问题 | 解决方法 |
|------|---------|
| 刷新设备列表为空 | 检查 USB 调试是否开启、USB 线是否支持数据传输 |
| 配对失败 | 确认输入的是**配对页面**的 IP:端口，不是无线调试主页的端口 |
| 连接被拒绝 (10061) | 确认已先完成配对，且使用的是**无线调试主页**的 IP:端口 |
| 设备显示"未授权" | 检查手机上是否有未响应的 USB 调试授权对话框 |
| 文件列表为空 | B 站缓存路径可能不同，用"上级目录"逐级查找 |

---

## 安卓设备开启开发者模式

### 通用步骤

所有安卓设备开启开发者模式的原理相同：

1. 打开 **设置** → **关于手机**（或"关于本机"）
2. 找到 **版本号**（或"软件版本号"、"MIUI 版本"等）
3. **连续快速点击 7 次**，直到提示"您已处于开发者模式"
4. 返回设置，在 **系统** 或 **其他设置** 中找到 **开发者选项**

> 以下为各品牌系统的具体路径差异。

### 各品牌系统详细路径

#### 小米 / 红米（HyperOS / MIUI）

1. 设置 → **我的设备** → **全部参数与信息**
2. 连续点击 **MIUI 版本** 或 **OS 版本** 7 次
3. 返回设置 → **更多设置** → **开发者选项**
4. 开启 **USB 调试**
5. 无线调试：开发者选项 → **无线调试**（Android 11+）

#### OPPO / 一加（ColorOS）

1. 设置 → **关于本机** → **版本信息**
2. 连续点击 **版本号** 7 次
3. 返回设置 → **系统** → **开发者选项**
4. 开启 **USB 调试**
5. 无线调试：开发者选项 → **无线调试**

> **一加 Ace 3V 实测**：ColorOS 系统，路径与上述一致。无线调试配对时注意区分配对端口与连接端口。

#### vivo / iQOO（OriginOS）

1. 设置 → **系统管理** → **关于手机**
2. 连续点击 **软件版本号** 7 次
3. 返回设置 → **系统管理** → **开发者选项**
4. 开启 **USB 调试**
5. 无线调试：开发者选项 → **无线调试**（Android 11+）

#### 华为（HarmonyOS / EMUI）

1. 设置 → **关于手机**
2. 连续点击 **版本号** 7 次
3. 返回设置 → **系统和更新** → **开发人员选项**
4. 开启 **USB 调试**

> **注意**：HarmonyOS 3.0+ 可能限制无线调试功能。部分华为设备需登录华为账号才能开启开发者选项。

#### 荣耀（MagicOS）

1. 设置 → **关于手机**
2. 连续点击 **版本号** 7 次
3. 返回设置 → **系统和更新** → **开发人员选项**
4. 开启 **USB 调试**

#### 三星（One UI）

1. 设置 → **关于手机** → **软件信息**
2. 连续点击 **版本号** 7 次
3. 返回设置 → **开发者选项**（出现在设置底部）
4. 开启 **USB 调试**
5. 无线调试：开发者选项 → **无线调试**（Android 11+）

#### 魅族（Flyme）

1. 设置 → **关于手机**
2. 连续点击 **版本号** 7 次
3. 返回设置 → **辅助功能** → **开发者选项**
4. 开启 **USB 调试**

#### realme（realme UI）

1. 设置 → **关于手机** → **版本信息**
2. 连续点击 **版本号** 7 次
3. 返回设置 → **系统** → **开发者选项**
4. 开启 **USB 调试**

#### 原生 Android（Pixel / 摩托罗拉 / 索尼等）

1. 设置 → **关于手机** → **版本号**
2. 连续点击 **版本号** 7 次
3. 返回设置 → **系统** → **开发者选项**
4. 开启 **USB 调试**
5. 无线调试：开发者选项 → **无线调试**（Android 11+）

---

## 缓存管理

Memoria 对从外部设备拉取的缓存文件进行生命周期管理，避免磁盘空间浪费。

### 缓存存储位置

```
C:\Users\<用户名>\AppData\Local\Memoria_N_V2\Memoria_N_V2\adb_cache\
```

> 使用 AppLocalData 目录，不会被系统自动清理，确保缓存有效。

### 自动清理策略

| 时机 | 动作 | 说明 |
|------|------|------|
| 混流导出成功后 | 删除该项目的缓存 | 文件已产出，缓存不再需要 |
| 从表格删除数据行 | 删除该行对应的缓存 | 用户不再需要 |
| 应用启动时 | 清理过期缓存 | 崩溃恢复 + 过期清理 |
| 应用关闭时 | 按设置清理 | 可选：全量清理或过期清理 |

### 手动管理

在 **设置 → 缓存管理** 选项卡中：

- **关闭时清理全部缓存**：勾选后每次关闭应用时全量清理（最省空间）
- **缓存保留 N 天后自动清理**：不勾选上述选项时生效，默认 7 天，可设 1-30 天
- **手动清理**：显示当前缓存大小，一键清理全部缓存

> **本地文件导入**的数据不受缓存管理影响 — 仅 ADB 拉取的文件受生命周期管理。

---

## 导出格式支持

| 格式 | 扩展名 | 处理方式 | 说明 |
|------|--------|---------|------|
| MP4  | `.mp4` | 流复制 | 最通用，+faststart 优化网络播放 |
| MKV  | `.mkv` | 流复制 | 开源容器，支持多轨 |
| MOV  | `.mov` | 流复制 | Apple 生态兼容 |
| WEBM | `.webm` | 转码 | VP9 视频 + Opus 音频，可选 CRF/码率/线程 |

> 流复制模式（MP4/MKV/MOV）不重新编码，导出速度接近文件复制速度。WEBM 需要转码，耗时较长。

---


### 模块职责

| 模块 | 职责 |
|------|------|
| CacheFileParser | 解析 B 站缓存文件（entry.json / index.json → ParsedCacheData） |
| AdbModule | ADB 设备管理（发现/配对/连接/目录浏览/文件拉取） |
| CacheManager | 缓存生命周期管理（创建/删除/过期清理/大小计算） |
| FFmpeg_module | 音视频混流与转码（QProcess 异步执行） |
| TaskQueue | 批量混流任务调度（顺序执行 + 进度反馈） |
| DataModel | 表格数据模型（18 列元数据 + 行状态管理） |

---

## 开发环境

- **语言**：C++ 17
- **框架**：Qt 6.11.1 (MinGW 64-bit)
- **构建**：CMake 3.16+
- **工具链**：MinGW 13.10 + CMake
- **外部依赖**：FFmpeg（随包）、ADB Platform Tools（随包）

---

## 许可证

本项目仅供学习和个人使用。请勿将导出的内容用于商业用途或侵犯版权。
